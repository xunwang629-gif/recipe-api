services:
  - type: web
    name: recipe-api
    env: docker
    dockerfilePath: ./Dockerfile
    # 环境变量将在 Render Dashboard 手动配置
    # 需要配置的变量: DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD, AUTO_APPLY_EVOLUTIONS
